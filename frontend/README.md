# frontend/

Put your Amplify-hosted frontend source here (index.html / style.css / script.js,
or a src/ folder if you're using a framework). This should be whatever calls
`GET /funfact` on your API Gateway endpoint and renders the response.
